name: HUMEAN Debug Test
on: workflow_dispatch
jobs:
  debug-test:
    runs-on: [self-hosted, windows]
    steps:
      - name: Test PowerShell
        shell: pwsh
        run: |
          Write-Host "✅ PowerShell fonctionne"
          Write-Host "📁 Dossier courant: $(Get-Location)"
          Get-ChildItem
